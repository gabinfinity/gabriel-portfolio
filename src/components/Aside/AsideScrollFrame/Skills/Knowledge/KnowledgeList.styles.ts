import styled from "styled-components";

export const Knowledge = styled.div`
    margin-bottom: 5px;
    display: flex;
    gap: 10px;
    padding: 0 10px;

    svg {
            height: auto;
            width: 12px;
            fill: #fec02f;
        }

    li {
      list-style-type: none;
      color: #8c8c8e;
}

`